"use client";

import { motion } from "motion/react";

/**
 * Re-export the motion components used across the app from a single place.
 */
export const MotionDiv = motion.div;
export const MotionSpan = motion.span;
